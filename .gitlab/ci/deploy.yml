# GitLab Pages Deployment
pages:
  stage: deploy
  dependencies:
    - build:docs
  image: alpine:latest
  script:
    - mv apps/documentation/build public
  artifacts:
    paths:
      - public
  rules:
    - if: $CI_COMMIT_BRANCH == "main"
